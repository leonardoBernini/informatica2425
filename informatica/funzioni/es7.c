/*numeri amicali
data una sequenza di valori verificare se i valori presi aq coppie si termina quando uno dei valori è 0*/
#include <stdio.h>
int amicali(int n,int n2);
int main(){
    int n,n2;
    printf("inserisci due valori");
    scanf("%d%d",n,n2);
    while(n==0, n2==0){
        
        printf("inserisci due valori");
        scanf("%d%d",n,n2);
    }
}